# Pixel Sprite Animator 技术收尾复盘

日期：2026-09-20

本文基于当前本地代码，记录导出、透明视频、切割、构图和底部动画条的实际实现。设计目标和当前代码行为分开描述，避免把计划功能误认为已经完成。

## 一、整体链路

```text
内置/导入 Sprite Sheet
        ↓
框选区域
        ↓
自动识别连通像素区域
        ↓
切割、抠底、紧边裁剪、底部中心锚点
        ↓
Frame + Animation + Layer
        ↓
Sprite Frame 播放 + Motion 位移 + 图层合成
        ↓
统一采样 Export Samples
        ↓
透明 PNG / PNG Sequence / GIF / ProRes 4444 MOV
```

当前系统的核心原则是：服务器只提供网页和资源，动画处理、画布合成和视频编码都在使用者自己的浏览器中执行。

## 二、从别人电脑导出视频

### 已完成

- 访问者打开网页后，资源通过网页加载到其浏览器。
- Sprite Sheet 解码、帧切割、透明合成、Motion Bake 都由浏览器 Canvas 完成。
- GIF 使用浏览器内的 GIF 编码器。
- MOV 使用浏览器内的 FFmpeg WebAssembly。
- 服务端 `/api/export-mov` 和 `/api/export-gif` 已关闭，返回 `BROWSER_LOCAL_EXPORT_ONLY`，不会接收视频帧。
- `/api/status` 返回 `exportMode: browser-local`，用于审计导出链路。

### MOV 的实际链路

1. 根据当前动作生成完整采样帧。
2. 每帧生成透明 PNG Canvas。
3. PNG 写入浏览器内存中的 FFmpeg WASM 虚拟文件系统。
4. 使用 `prores_aw`、ProRes 4444 Profile 4、`yuva444p10le` 编码。
5. 读取内存中的 MOV，并触发该用户电脑的下载。
6. 编码结束后删除临时帧和临时 MOV。

### 现实限制

- MOV 编码需要浏览器内存同时容纳 WASM、PNG 帧和输出缓冲区；高分辨率、长动作、20 倍持续时间会明显增加内存压力。
- Safari、Chrome、Premiere 对 ProRes 4444 的解码支持不同，导出文件本身是透明 ProRes，但播放预览不应只依赖浏览器内置播放器判断。
- 服务器可以保持常驻，但不能替使用者电脑承担浏览器编码性能。

## 三、透明视频和图片导出

### 统一采样

`expandedExportFrames()` 根据：

- 当前动作帧顺序
- 每帧 `duration`
- 导出帧持续倍率
- 循环次数

生成导出采样序列，并给每个采样计算 `motionT`。之后 PNG、GIF 和 MOV 都从这套采样继续处理，避免三种格式使用三套不同的动画逻辑。

### 透明画布

`exportUnit()` 遍历全部采样帧和 Motion 位移，计算 Union Bounding Box：

- 帧透明像素范围
- Anchor
- 每一帧的位移位置
- 跳跃最高点
- 终点位置
- Padding

然后 `drawAligned()` 使用 `imageSmoothingEnabled = false` 和 Pixel Snap，将帧绘制到透明画布中。

### 各格式

- 当前帧 PNG：透明 Canvas，按最小 800×900 规则放大。
- PNG Sequence：每帧独立透明 PNG，Motion 已 Bake 进去。
- GIF：浏览器本地 GIF 编码，使用透明索引和调色板量化；GIF 允许一定程度的颜色和透明边缘损失。
- MOV：浏览器 FFmpeg WASM 编码为透明 ProRes 4444。

### 当前需要记录的收尾问题

1. 导出菜单的“当前帧 PNG”目前调用 `exportCurrentFramePng()`，不是固定 3840×2160 的 `exportPng4K()`。4K 函数存在，但当前菜单没有直接接线。
2. 有限 GIF 循环会在采样阶段复制帧，但编码器对非零循环值仍写入 `repeat: -1`，因此实际可能表现为内容重复后继续无限循环。需要统一 GIF loop extension 与采样复制策略。
3. MOV 和 PNG Sequence 没有“播放器循环元数据”，循环次数通过复制采样帧实现；这对 MOV 是正确的时长表达，但不是编辑软件里的循环标记。

## 四、切割算法

### 识别流程

`detectBoxesInRegion()` 当前执行：

1. 将选区像素读入 Canvas。
2. 以 2 像素步长扫描连通区域。
3. 使用当前素材左上角背景色判断前景像素。
4. 记录每个连通区域的边界框。
5. 合并相邻的手、脚、头发和投射物区域。
6. 对过长的大框，在明显空列处再次拆分。
7. 过滤靠近上下边缘、过窄、过浅的文字和标签。
8. 按行和 X 坐标排序，生成帧顺序。

### 单帧抠图流程

`cropFrameImproved()` 当前执行：

- 取选区边缘颜色和边角颜色。
- 使用边缘连通 Flood Fill 清除蓝色/背景色。
- 使用底部颜色采样清除底部连通颜色。
- 识别低矮、横向、接近透明区域的绿色地面或阴影条。
- 对透明边缘附近的相似颜色做保守抗锯齿处理。
- 重新计算非透明像素的紧边界。
- Anchor 自动设为底部中心附近。

设置面板中的边缘容差和底部容差会自动重新处理当前动作，而不是只改变显示数字。

### 当前风险

- 不同来源的 Sprite Sheet 背景色、文字、版本说明可能不同，自动识别不可能完全替代人工确认。
- 如果角色本身带有低矮、深绿色的脚部特效，底部颜色算法仍需要降低容差或关闭底部清理。
- 连通区域识别依赖像素之间的间隔；完全断开的投射物可能被识别为独立帧或被过滤，需要人工复核。

## 五、构图和 Motion 算法

### Frame、Animation、Layer

- `state.frames` 保存所有全局帧对象。
- `animation.frameIds` 保存当前动作的帧顺序。
- `layer.frameIds` 保存某一图层使用的帧集合。
- Frame 保存原始选区坐标、原始宽高、裁剪后 Canvas、透明 PNG、Anchor 和持续时间。
- Layer 保存可见性、透明度、X/Y 偏移和逐帧可见状态。

### 预览构图

`compositionEntries()` 以当前 `playIndex` 从各图层取出当前帧，再由 `drawPreview()` 合成：

```text
当前 Frame
    + 图层 X/Y 偏移
    + Motion Position
    + Anchor
    + Pixel Snap
    ↓
预览 Canvas
```

### Motion

- Static：位置不变。
- Linear：起点到终点线性插值。
- Jump：在线性 X 位移上叠加抛物线 Y 位移。
- Custom：使用二次贝塞尔控制点。
- 微小位移：额外叠加可关闭的像素级正弦偏移。

导出时使用 `exportMotionPosition()`，所以导出的不是原地 Sprite，而是已经包含位移的帧序列。

### Union Bounds 和 Pixel Perfect

导出边界会遍历整个 Motion 过程，避免裁掉起点、终点和跳跃最高点。每帧的最终绘制坐标默认 Round 到整数像素，避免运动过程中的 Subpixel Blur 和 Pixel Shimmer。

### 当前需要记录的收尾问题

- 图层 UI 有“始终循环”和“跟随关键帧”两种模式，但当前 `compositionEntries()` 的基础取帧逻辑仍以全局 `playIndex % layer.frameIds.length` 为主，两个模式的时间语义还没有完全分离。
- 当前是轻量图层合成，不是完整的多轨道时间轴；图层之间还没有独立起始时间、延迟、速度和事件触发器。

## 六、底部动画条逻辑

### 当前交互

- Frame 卡片对应 `activeFrames()` 的顺序。
- 拖动卡片会调整 `animation.frameIds` 顺序。
- 点击卡片切换当前帧。
- Shift 点击支持多选。
- 空白区域框选支持批量选择。
- 每张卡片右下角可以直接删除。
- 图层时间轴显示每个图层在每个 Frame 上是否有内容。
- Motion 行显示从起点到终点的运动进度。

### 播放时钟

播放间隔基本公式为：

```text
interval = 1000 / FPS × frame.duration / previewSpeed
```

每次 Tick：

1. 根据当前帧持续时间判断是否换帧。
2. 更新 `playIndex`。
3. 用最后一帧索引计算 `motionProgress`。
4. 循环时回到第 0 帧。
5. 非循环时停在最后一帧。

### 当前限制

- Frame 数据有 `duration` 字段，但当前页面没有完整的逐帧持续时间编辑控件，主要仍依赖全局 FPS、预览速度和导出倍率。
- 底部条是 Frame First 的轻量编排器，不是传统按秒编辑的 NLE 时间轴。
- 图层时间轴当前主要控制帧归属、显示和排序，还没有完整的独立轨道时钟。

## 七、综合结论

当前系统已经完成了一个可交付的浏览器本地像素动画闭环：

- 别人的电脑可以独立导出。
- 透明 PNG、PNG Sequence、透明 GIF、透明 ProRes 4444 MOV 都走本地浏览器链路。
- Sprite Sheet 可以自动识别、切割、抠底和紧边裁剪。
- Motion 会进入预览和导出边界计算。
- 底部动画条可以进行排序、选择、删除、图层显示和基础播放。

下一阶段如果只做技术收尾，建议按以下顺序处理：

1. 修正有限 GIF 循环语义。
2. 将菜单中的当前帧 PNG 明确接到 4K 导出，或把按钮命名为“最小 800×900 PNG”。
3. 给每帧增加明确的持续时间编辑控件，并让预览和导出共用同一时间采样器。
4. 分离图层的独立时间轴、起始时间和循环模式。
5. 对自动切割增加“保留/忽略该区域”的人工修正入口。
6. MOV 长动画改为分块写入或增加内存预估，降低 Safari 浏览器崩溃风险。

总体判断：核心架构已经成立，当前主要问题不在“能不能导出”，而在“导出参数语义、GIF 循环、4K 按钮接线和多图层时间轴”需要统一收口。
