# Pixel Sprite Animator：动画编排与透明导出链路策划案

## 目标

下一阶段的重点不是增加更多单独按钮，而是让以下三件事使用同一套时间模型：

1. 动态预览
2. 时间轴编排
3. PNG / GIF / MOV / PNG Sequence 导出

最终关系应该是：

```text
时间轴时间 t
    ↓
动画片段采样
    ↓
Sprite Frame + Layer + Motion + 事件
    ↓
统一合成画面
    ├─ 预览画布
    └─ 导出编码器
```

预览和导出不能各自计算一套位置。只要同一个时间点在预览中正确，导出的每一帧也必须使用同一个结果。

## 当前版本已经具备的基础

当前实现可以作为下一版底座，不需要推翻重写。

### Sprite Frame

- 框选 Sprite Sheet 后自动检测连续帧。
- 每一帧会做背景清理。
- 非透明像素会进行紧边裁切。
- 每一帧保存 `canvas`、尺寸、来源 Sheet 和自动底部中心锚点。
- 每一帧有 `duration`，可用于控制停留时间。

### Layer

- 每个 Sprite Sheet 可以对应一个图层。
- 图层有显示 / 隐藏、透明度、X / Y 偏移。
- 图层可以设置为“始终循环”或“跟随关键帧”。
- 预览区可以直接拖动图层。

### Motion

当前 Motion 有四种概念：

- `static`：原地播放。
- `linear`：起点到终点的直线插值。
- `jump`：在起点和终点之间叠加抛物线高度。
- `custom`：使用二次贝塞尔控制点。

Motion 最终会转成一个位置函数：

```text
position = motionPosition(t)
```

其中 `t` 是 0 到 1 的归一化进度。

### 预览

当前预览流程大致是：

```text
当前播放帧
    ↓
计算 Motion 位置
    ↓
计算每个图层位置
    ↓
Nearest Neighbor 绘制 Sprite
    ↓
绘制网格、Motion 轨迹和图层控制点
```

预览支持播放、暂停、单帧前进、单帧后退、拖动视图和滚轮缩放。

### 当前导出

现有导出入口包括：

- 当前帧透明 PNG。
- 全部动作帧 PNG。
- 透明 PNG Sequence。
- 透明 GIF。
- 透明 MOV。
- 修理日志。

所有导出均在访客浏览器中完成，服务端不接收帧，也不调用 Mac FFmpeg。

### 当前实现需要留意的边界

当前版本适合“一个当前动作 + 多个图层”的编排，但还不是完整的多轨道动画编辑器：

- `compositionEntries()` 主要依据全局 `playIndex` 取帧，两个动作还没有各自独立的时间游标。
- `exportUnit()` 主要依据当前 Action 的 Motion 计算画布边界，多角色合成时需要改为遍历全部轨道。
- `expandedExportFrames()` 会先展开循环，再把 `motionT` 均匀分配到整个导出列表。下一版应明确 Sprite Loop 和 Motion Loop 是否分别重置。
- 预览目前以当前帧为中心，下一版需要改为以项目 `currentTime` 采样全部轨道。

这些不是现有导出坏掉，而是从单动作模型升级到动画编排器时必须抽离的边界。

## 当前导出链路

### 1. 收集动作帧

导出先读取当前 Action 的 `activeFrames()`。

每一帧包含：

```js
{
  id,
  canvas,
  w,
  h,
  anchorX,
  anchorY,
  duration,
  sourceSheetId
}
```

### 2. 展开帧持续和循环

导出设置目前包括：

- 帧持续倍率：1×、3×、5×、8×、10×、15×、20×。
- 循环次数：1、2、3、5、10 或 GIF 无限循环。

展开后得到导出采样列表：

```text
Frame 01 × duration
Frame 02 × duration
Frame 03 × duration
...
```

每个采样点会生成一个 `motionT`，再交给 Motion 计算位置。

### 3. 计算运动边界

导出不会直接使用单帧尺寸，而是遍历全部采样点：

```text
每一帧 Sprite 的像素矩形
+ Anchor
+ Motion Position
+ Layer Offset
+ Padding
-----------------------------
Union Bounding Box
```

这样可以避免：

- 跳跃最高点被裁掉。
- 终点位置被裁掉。
- 角色脚部超出画布。
- 多图层之间互相覆盖到画布外。

### 4. 统一透明画布

得到 Union Bounding Box 后，所有导出帧使用相同的画布尺寸和相同的原点。

每帧绘制时：

```text
最终 X = (Motion X - Anchor X - Union Min X) × Scale
最终 Y = (Motion Y - Anchor Y - Union Min Y) × Scale
```

如果 `pixelSnap = true`，最终 X / Y 会取整，避免像素抖动。

### 5. 生成不同格式

#### PNG

- 单帧：导出当前选中的动作帧。
- 全部帧：逐帧导出透明 PNG。
- PNG Sequence：使用相同 Union Bounding Box 逐帧导出。
- 使用 Nearest Neighbor。
- 不应重复压缩 PNG。

#### GIF

- 使用同一组透明画布。
- 使用调色板量化。
- 保留透明索引。
- GIF 可以降低颜色精度，但不能改变帧顺序、位置和持续时间。
- 无限循环只应影响 GIF 播放标记，不应复制无限数量的帧。

#### MOV

- 通过浏览器 `MediaRecorder` 和 `canvas.captureStream()` 生成。
- 优先使用支持透明通道的 QuickTime / ProRes MIME。
- 导出前要记录浏览器是否支持透明 MOV MIME。
- 如果浏览器不支持透明 MOV，应明确提示失败原因，并保留修理日志。

## 下一阶段的核心升级：动画编排器

### 核心原则

不要把第二个动画直接复制进第一组 Frame。

应该建立“轨道”和“动画片段”：

```text
Track A：隆 · 出拳
Track B：对手 · 受击
Track C：火花 / 特效
Track D：Motion / Camera
```

每条轨道可以放一个或多个 Clip。

### 推荐数据结构

```js
{
  projectFps: 24,
  duration: 2.4,
  tracks: [
    {
      id: 'track-ryu',
      name: '隆',
      type: 'sprite',
      zIndex: 10,
      visible: true,
      clips: [
        {
          id: 'clip-ryu-punch',
          animationId: '动作 1',
          start: 0.0,
          duration: 0.8,
          sourceIn: 0,
          sourceOut: 7,
          playbackRate: 1,
          loop: 'once',
          offsetX: 0,
          offsetY: 0,
          opacity: 1,
          blend: 'normal'
        }
      ]
    },
    {
      id: 'track-opponent',
      name: '对手',
      type: 'sprite',
      zIndex: 20,
      visible: true,
      clips: []
    }
  ],
  markers: [],
  interactions: []
}
```

### 时间单位

底层统一使用秒，界面同时显示帧号：

```text
time = 0.500 秒
frame = round(time × projectFps) = 12
```

建议：

- 默认项目 FPS：24。
- 预览可以慢速播放，但不改变项目时间。
- 时间轴吸附单位为 1 帧。
- 用户可以看到 `00:00.000`、`00:00.500` 这样的时间戳。
- 导出时使用项目 FPS，不直接使用界面慢速倍率。

### Sprite Clip 采样

对于某个时间点 `t`：

```text
localTime = (t - clip.start) × clip.playbackRate
frameIndex = frameAt(localTime, clip.sourceIn, clip.sourceOut, clip.loop)
```

Loop 类型建议：

- `once`：播放一次，结束后停在最后一帧或隐藏。
- `loop`：循环播放。
- `pingpong`：正向后反向。
- `hold`：只显示第一帧，直到片段结束。

### Motion Clip 采样

Motion 不应该绑定到某个全局 `playIndex`，而应该绑定到 Clip 或 Track：

```text
motionT = clamp((t - clip.start) / clip.duration, 0, 1)
position = motionPosition(motionT, clip.motion)
```

这样 Sprite Frame 和 Motion 可以分别循环：

```text
Sprite：loop
Motion：once
```

例如走路动画可以一直循环，但角色整体位置可以从 X=0 移动到 X=1000，不能因为 Sprite 循环而重置位置。

## 两个动画之间的反应

### 推荐的第一版互动模型

先不要做复杂物理系统，只做“事件 + 时间偏移”：

```text
隆 · 出拳
        │
        └── 触发：hit
                    │ delay = 0.08s
                    ↓
对手 · 受击
```

事件结构：

```js
{
  id: 'event-hit-1',
  sourceClipId: 'clip-ryu-punch',
  sourceTime: 0.34,
  type: 'hit',
  targets: [
    {
      trackId: 'track-opponent',
      action: '受击',
      delay: 0.08,
      mode: 'replace'
    }
  ]
}
```

### 互动呈现方式

当播放头到达事件时间：

1. 主动画继续播放。
2. 目标动画在事件时间 + delay 处开始。
3. 目标动画可以覆盖当前动作，也可以追加到当前动作后面。
4. 目标轨道保留自己的图层位置、Motion 和循环设置。

### 第一阶段只实现三种反应

- `hit`：受击动作。
- `land`：落地动作。
- `spawn`：生成特效或第二个动画。

先不加入碰撞检测。用户在时间轴上放置事件标记即可，结果更可控。

## 统一评估器

下一版最重要的底层函数应该是：

```js
evaluateComposition(project, time)
```

返回某个时间点的完整画面状态：

```js
{
  time: 0.5,
  width: 640,
  height: 640,
  entries: [
    {
      frame,
      x,
      y,
      opacity,
      zIndex,
      visible,
      blend,
      pixelSnap: true
    }
  ],
  markers: [],
  bounds: {}
}
```

预览和导出都只做两件事：

```js
const scene = evaluateComposition(project, time);
drawScene(previewCtx, scene);
```

或：

```js
const scene = evaluateComposition(project, time);
drawScene(exportCtx, scene);
```

不要在 `drawPreview()` 和 `exportLocalGif()` 里分别写一套动画计算。

## 新版导出链路

### Export Plan

点击导出后，先生成一个不可变的导出计划：

```js
{
  fps: 24,
  duration: 2.4,
  totalFrames: 58,
  loopMode: 'once',
  scale: 8,
  padding: 10,
  transparent: true,
  pixelSnap: true,
  format: 'png-sequence'
}
```

导出计划一旦开始，不再读取用户正在拖动的控件，避免导出过程中画面变化。

### 采样全部时间点

```text
for frame = 0 ... totalFrames - 1:
    time = frame / fps
    scene = evaluateComposition(project, time)
    保存 scene.entries
```

### 计算全局边界

对所有 scene 的所有 entries 取并集：

```text
minX = min(entry.x - entry.frame.anchorX)
minY = min(entry.y - entry.frame.anchorY)
maxX = max(entry.x - entry.frame.anchorX + entry.frame.w)
maxY = max(entry.y - entry.frame.anchorY + entry.frame.h)
```

然后再加 Padding。

重要：边界必须在所有动画轨道合并后计算，不能每个角色单独算画布，否则两个动画互动时会出现裁切或画面漂移。

### 渲染统一帧

所有格式都应该从同一组 `RenderedFrame` 生成：

```js
{
  index: 12,
  time: 0.5,
  canvas,
  width,
  height,
  durationMs: 41.667
}
```

导出器只负责编码，不再负责角色位置计算。

## 三种格式的建议定位

### PNG Sequence

作为最高可靠的母版格式：

- 透明通道完整。
- 每帧独立。
- 适合后期软件继续编排。
- 可以保留严格像素边缘。

### GIF

作为快速预览和分享格式：

- 允许颜色减少。
- 保留透明背景。
- 支持无限循环。
- 不适合作为高清母版。

### MOV

作为剪辑软件使用格式：

- 透明通道优先。
- 使用项目 FPS。
- 预览中的空间位移必须已经 Bake 进去。
- 导出前检查浏览器是否支持透明 MIME。

## 基础样式策划案

### 一级：区域

页面只保留五个主要区域：

1. 素材 / Action / Layer
2. Sprite Sheet 编辑
3. 动态预览
4. 时间轴编排
5. 导出

区域之间使用背景明度、边框和留白区分，不使用大段说明文字。

### 二级：功能

二级功能保持少量固定入口：

- 导入 Sprite
- 新建 Action
- 新建 Layer
- 播放 / 暂停
- 添加时间轴片段
- 添加事件标记
- 导出

### 三级：参数

参数只在当前选择对象的 Inspector 中出现：

- Action：FPS、循环、总时长。
- Clip：开始时间、结束时间、速度、循环方式。
- Layer：X、Y、透明度、层级。
- Motion：起点、终点、跳跃高度、Pixel Snap。
- Export：格式、FPS、尺寸、Padding、循环。

同一个参数不要在预览区、时间轴和导出区重复出现。

### 时间轴视觉

推荐结构：

```text
时间刻度   0.0s    0.5s    1.0s    1.5s
隆         [Clip: 出拳              ]
对手                         [Clip: 受击       ]
特效              ◆ hit
Motion      ●──────────────●
播放头             │
```

视觉规则：

- 黄色：当前播放头和当前编辑对象。
- 绿色：Motion 和已启用图层。
- 红色：删除、错误和危险操作。
- 灰紫色：普通轨道和未选中状态。

## 推荐实施顺序

### 第 1 阶段：时间轴基础

- 给项目增加 `projectFps` 和 `duration`。
- 给每个 Action 增加 Clip 包装层。
- Frame 仍然使用现有数据。
- 先把 `playIndex` 转换为 `time`。

### 第 2 阶段：双动画同屏

- 新增第二条 Sprite Track。
- 两个 Clip 可以设置不同开始时间。
- 先支持静态位置和图层层级。
- 预览能够同时显示两个动作。

### 第 3 阶段：Motion 独立

- Motion 绑定到 Clip 或 Track。
- Sprite Loop 和 Motion Loop 分开。
- 预览轨迹和导出轨迹都调用同一个 `evaluateComposition()`。

### 第 4 阶段：事件反应

- 增加时间轴 Marker。
- 支持 `hit`、`land`、`spawn`。
- 事件触发目标 Clip 的开始、替换或追加。

### 第 5 阶段：统一导出

- 先生成 Export Plan。
- 统一采样全部时间点。
- 统一计算多轨道 Union Bounds。
- 统一生成 RenderedFrame。
- 最后分别编码 PNG Sequence、GIF、MOV。

### 第 6 阶段：界面收口

- 时间轴作为第二视觉中心。
- 预览只显示当前画面和必要控制点。
- 右侧 Inspector 根据当前选择对象切换。
- 导出保留一个主入口。

## 验收标准

### 双动画

- 角色 A 和角色 B 可以同时存在于不同轨道。
- 两个动画可以有不同开始时间和持续时间。
- 一个动画循环时，不会重置另一个动画的位置。
- 图层顺序稳定，导出与预览一致。

### 互动

- 时间轴上可以放置 `hit` 标记。
- 目标动作可以设置反应延迟。
- 播放时能够看到动作先后关系。
- 不需要碰撞检测也能稳定复现。

### 导出

- 预览画面和导出画面逐帧一致。
- 多角色同时运动时不会裁切。
- 所有格式共享同一组采样帧。
- PNG Sequence 是透明母版。
- GIF 是透明预览版。
- MOV 已经 Bake 空间位移，不需要在剪辑软件里重新打位置关键帧。

### 像素质量

- 统一使用 Nearest Neighbor。
- 默认 Pixel Snap 开启。
- 所有导出帧使用相同画布尺寸。
- 不因 Subpixel Motion 产生边缘忽清晰忽模糊。

## 本轮建议只做的小升级

如果希望保持风险最低，第一轮只实现下面四项：

1. 增加 `projectFps`、`currentTime` 和时间刻度。
2. 增加第二条 Sprite Track，让两个现有 Action 可以同屏。
3. 增加 Clip `start`、`duration`、`playbackRate`。
4. 把预览和导出共同改为调用一个 `evaluateComposition(project, time)`。

先不要加入复杂碰撞、物理反弹、音频、摄像机系统或新的编码器。只要这四项稳定，后续的互动和导出升级就有可靠基础。
