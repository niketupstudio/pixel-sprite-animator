# Pixel Sprite Animator 导出与时间轴审计报告

审计范围：公网版本、`4174` 本地版本、透明素材处理、浏览器本地导出、动作时间轴与预览拖动。

## 已发现并修复

### 透明背景

问题：背景色检测后，修改后的像素没有重新写回裁切 Canvas，因此部分导出仍然带绿色底。

修复：裁切流程现在会从选区边缘采集多组背景颜色，使用自适应颜色距离做边缘连通抠除；随后只对紧邻透明区域的抗锯齿色边做半透明清理，最后再写回 RGBA 并紧边裁切。这样可以清掉大幅动作四角和轮廓边缘残留的底色，同时避免把角色主体中的绿色特效整体抠除。生成帧、PNG、PNG Sequence、GIF、MOV 共用这套透明帧数据。

### 导出归属

公网和本地页面都使用浏览器端 Canvas、GIF 编码器和视频编码能力。服务端不接收导出帧，不调用 Mac FFmpeg。旧服务端编码接口继续返回 `410 BROWSER_LOCAL_EXPORT_ONLY`。

### 导出内容

- 当前帧透明 PNG
- 全部动作帧透明 PNG
- 透明 PNG Sequence
- 透明 GIF
- 透明 MOV

导出参数从当前页面读取，包括动作帧、FPS、预览速度、动作位移、帧持续倍率和循环次数。PNG 使用非透明像素紧边裁切；GIF/MOV/Sequence 使用完整运动画布并保持透明通道。

### 动作时间轴

“基于当前图层新建动作”会创建独立的第二条时间轴，并切换到新时间轴。之后“追加到当前动作”只修改当前选中的时间轴。每个真实加入的 Sprite Sheet 才会生成图层，不再预置背景、固定装饰、动作帧占位层。

### 预览拖动

动态预览会显示实际图层拖动手柄。拖动图层手柄会修改该图层的 X/Y，不会只移动预览相机；运动轨迹手柄仍用于编辑整体 Motion。

基于当前图层新建动作会在当前时间轴中创建第二个真实图层，并把新生成帧绑定到该图层，使两层同时循环。

### 输出尺寸

当前帧 PNG、PNG Sequence、GIF 和 MOV 的浏览器端导出会自动选择整数像素倍率，保证导出画布至少达到 `800×900` 级别，同时保持 Nearest Neighbor 和透明通道。PNG 仍按精灵非透明像素紧边裁切；动画格式会保留运动 union bounds。

### 帧批量编辑

帧列表支持 `Alt + 拖动框选`、`Shift + 点击多选`，并通过“删除所选帧”批量删除，同时清理动作时间轴和图层引用。

## 透明 MOV 编码改进

浏览器 `MediaRecorder.isTypeSupported()` 不代表浏览器能稳定生成 Premiere 可用的透明 ProRes MOV。修理日志中 Chrome 的 `ap4h`、`prores4444` 和 `prores` 均为 `false`，因此旧链路会正确拒绝导出，但无法满足跨浏览器需求。

本地版本现改为按需加载同源 FFmpeg WebAssembly，并在访问者自己的浏览器中执行：

- 输入：统一渲染后的透明 PNG 序列。
- 编码器：FFmpeg WebAssembly 可用的 `prores_aw`。
- Profile：ProRes 4444（`-profile:v 4`）。
- 像素格式：`yuva444p10le`，保留透明通道。
- 容器：QuickTime MOV。
- 服务端：不接收帧，不调用 Mac FFmpeg。

这条链路不依赖 Safari 的 `MediaRecorder` 是否声明透明 MOV MIME，Premiere 导入的是标准 ProRes 4444 QuickTime 文件。首次点击 MOV 会在当前电脑加载约 31 MB WebAssembly 编码器，之后复用浏览器内存中的编码器。

## 验证结果

- `node --check public/app.js`：通过
- `npm test`：通过
- 公网登录：通过，访问码由 `PIXEL_SPRITE_ACCESS_KEY` 配置（报告不记录实际访问码）
- 公网页面导出入口：PNG、PNG Sequence、GIF、MOV 均存在
- 公网页面不包含 H264、H256PS、MP4 入口
- 公网 `/api/status`：`exportMode=browser-local`
- 公网旧远程 MOV 接口：返回 `410 BROWSER_LOCAL_EXPORT_ONLY`
- 本地 `4174` 常驻服务：运行中

## 兼容性说明

GIF 在浏览器内量化颜色以控制文件大小。透明 MOV 只有在访问者浏览器声明支持透明 QuickTime 编码器时才会生成；不支持时页面生成修理日志，不会改用 Mac 算力或生成不透明伪文件。
